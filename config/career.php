<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Специализация для страницы «Карьера»
    |--------------------------------------------------------------------------
    | Одна специальность, заранее задаётся, без возможности менять пользователем.
    | Используется в запросах к API HH.RU (параметр text).
    */
    'vacancy_specialization' => env('CAREER_VACANCY_SPECIALIZATION', 'менеджер'),

    /*
    | Количество вакансий в выдаче (первые N из результатов поиска HH.RU).
    */
    'vacancy_count' => (int) env('CAREER_VACANCY_COUNT', 6),

    /*
    | Время жизни кэша результатов запроса к HH.RU (в минутах).
    */
    'cache_ttl_minutes' => (int) env('CAREER_CACHE_TTL_MINUTES', 20),
];
